<UserControl x:Class="CryptoBooz.Client.Views.ExchangesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="500" d:DesignWidth="700"
             DataContext="{Binding Exchanges, Source={StaticResource Locator}}">
    
    <DockPanel LastChildFill="True">
        <DockPanel DockPanel.Dock="Bottom" Background="Aquamarine" LastChildFill="False">
            <Button DockPanel.Dock="Right" Margin="4 6 4 6" Width="120"
                    Content="Сохранить"
                    Style="{StaticResource AccentedSquareButtonStyle}">
            </Button>
        </DockPanel>
        <ItemsControl DockPanel.Dock="Left" Width="200" Background="AntiqueWhite">
            
        </ItemsControl>
        <DataGrid>
            <DataGrid.Columns>
                <DataGridTextColumn Header="asdfasdfasd"></DataGridTextColumn>
                <DataGridComboBoxColumn Header="lkjlkj"></DataGridComboBoxColumn>
                <DataGridComboBoxColumn Header="9090909"></DataGridComboBoxColumn>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
    
</UserControl>
